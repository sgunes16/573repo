# ===========================================
# HIVE - Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values
# For detailed documentation, see DEPLOYMENT.md
# ===========================================

# -------------------------------------------
# Django Settings
# -------------------------------------------
SECRET_KEY=your-secret-key-here              # Generate: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DEBUG=True                                    # Set to False in production!
DEPLOY_TYPE=dev                               # dev or prod (affects cookie security)
FRONTEND_URL=http://localhost:3000            # Production: https://yourdomain.com

# -------------------------------------------
# Database (PostgreSQL)
# -------------------------------------------
DB_NAME=hive_db
DB_USER=postgres
DB_PASSWORD=your-db-password                  # Required: Change this!
DB_HOST=db                                    # Use 'db' for Docker, 'localhost' for local
DB_PORT=5432

# -------------------------------------------
# MinIO (S3-compatible File Storage)
# -------------------------------------------
MINIO_ROOT_USER=minioadmin                    # Change in production!
MINIO_ROOT_PASSWORD=your-minio-password       # Required: Change this!
MINIO_BUCKET_NAME=hive-media

# -------------------------------------------
# Email Settings (SMTP)
# -------------------------------------------
# Required for production (email verification, notifications)
# See THIRD_PARTY_SETUP.md for Gmail App Password setup
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465                                # 465 for SSL, 587 for TLS
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password         # Gmail: Use App Password, not account password!

# -------------------------------------------
# Mapbox (Map & Geocoding)
# -------------------------------------------
# Required for production (dashboard map, address autocomplete)
# See THIRD_PARTY_SETUP.md for token setup
VITE_MAPBOX_TOKEN=pk.your-mapbox-token-here

# -------------------------------------------
# Auto Bootstrap (Optional)
# -------------------------------------------
# Used by backend/entrypoint.sh on container start
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=change-me           # Change this!
RUN_SEED_OFFERS=true                          # Set to false in production

# ===========================================
# Production Checklist:
# ===========================================
# [ ] SECRET_KEY - Generate a unique key
# [ ] DEBUG=False
# [ ] DEPLOY_TYPE=prod
# [ ] FRONTEND_URL - Set your domain
# [ ] DB_PASSWORD - Strong password
# [ ] MINIO_ROOT_USER - Change from default
# [ ] MINIO_ROOT_PASSWORD - Strong password
# [ ] EMAIL_* - Configure for verification emails
# [ ] VITE_MAPBOX_TOKEN - Get from mapbox.com
# [ ] RUN_SEED_OFFERS=false - No demo data
# [ ] SSL certificates in nginx/ssl/
# ===========================================
