# ===========================================
# HIVE - Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values
# For detailed documentation, see DEPLOYMENT.md
# ===========================================

# -------------------------------------------
# Django Settings
# -------------------------------------------
SECRET_KEY=django-insecure-dev-key-change-in-prod    # For local dev only! Generate for prod: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DEBUG=True                                    # Set to False in production!
DEPLOY_TYPE=dev                               # dev or prod (affects cookie security)
FRONTEND_URL=http://localhost                 # Production: https://yourdomain.com

# -------------------------------------------
# Database (PostgreSQL)
# -------------------------------------------
DB_NAME=hive_db
DB_USER=postgres
DB_PASSWORD=postgres123                       # Change this in production!
DB_HOST=localhost                             # Use 'localhost' for local/hybrid, 'db' for full Docker (auto-set by compose)
DB_PORT=5432

# -------------------------------------------
# MinIO (S3-compatible File Storage)
# -------------------------------------------
MINIO_ROOT_USER=minioadmin                    # Change in production!
MINIO_ROOT_PASSWORD=minioadmin123             # Change this in production!
MINIO_BUCKET_NAME=hive-media

# -------------------------------------------
# Email Settings (Resend)
# -------------------------------------------
# Optional for local dev, required for production (email verification, notifications)
# Free tier: 3,000 emails/month, 1 custom domain
# See THIRD_PARTY_SETUP.md for setup guide
# For LOCAL DEV: Leave RESEND_API_KEY empty (email features won't work but app runs fine)
RESEND_API_KEY=                               # Get from https://resend.com/api-keys (optional for local)
RESEND_CUSTOM_DOMAIN=false                    # true = auto-generate: noreply@<FRONTEND_URL domain>
RESEND_FROM_EMAIL=onboarding@resend.dev       # Only used if RESEND_CUSTOM_DOMAIN=false

# -------------------------------------------
# Mapbox (Map & Geocoding)
# -------------------------------------------
# Required for production (dashboard map, address autocomplete)
# For LOCAL DEV: Use the shared dev token below (works on localhost)
# For PRODUCTION: Create your own token at mapbox.com (see THIRD_PARTY_SETUP.md)
VITE_MAPBOX_TOKEN=<public-token>

# -------------------------------------------
# Auto Bootstrap (Optional)
# -------------------------------------------
# Used by backend/entrypoint.sh on container start
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=change-me           # Change this!
RUN_SEED_OFFERS=true                          # Set to false in production

# ===========================================
# Production Checklist:
# ===========================================
# [ ] SECRET_KEY - Generate a unique key
# [ ] DEBUG=False
# [ ] DEPLOY_TYPE=prod
# [ ] FRONTEND_URL - Set your domain (https://yourdomain.com)
# [ ] DB_PASSWORD - Strong password
# [ ] MINIO_ROOT_USER - Change from default
# [ ] MINIO_ROOT_PASSWORD - Strong password
# [ ] RESEND_API_KEY - Get from resend.com
# [ ] RESEND_CUSTOM_DOMAIN=true - After setting up custom domain in Resend
# [ ] VITE_MAPBOX_TOKEN - Get from mapbox.com
# [ ] RUN_SEED_OFFERS=false - No demo data
# [ ] SSL certificates in nginx/ssl/
# ===========================================
